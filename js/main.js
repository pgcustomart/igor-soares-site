/*
  Ponto de entrada — os módulos em js/components/ são auto-inicializáveis
  (IIFEs carregados via <script defer>). Este arquivo existe para eventuais
  inicializações globais futuras, mantendo o index.html com uma única
  referência de "orquestração" ao final da lista de scripts.
*/
